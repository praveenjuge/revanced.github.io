const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    extend: {
      colors: {
        primary: "#ff4151",
        secondary: "#282a36",
        background: "#0f111a",
      },
    },
  },

  plugins: [],
};

module.exports = config;
